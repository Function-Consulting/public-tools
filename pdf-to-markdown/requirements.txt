pymupdf4llm>=0.0.10
pymupdf>=1.24
# Optional - only used to OCR scanned (image-only) PDFs before conversion.
# The tool auto-installs these on first use; the Tesseract engine must also
# be installed (https://github.com/UB-Mannheim/tesseract/wiki).
pytesseract>=0.3
Pillow>=10
